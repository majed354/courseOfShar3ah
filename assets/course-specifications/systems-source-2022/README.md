# توصيفات بكالوريوس الأنظمة

تضم هذه الحزمة 34 توصيفًا فريدًا: 30 ملفًا منفردًا لم تتغير في دفعة تصحيح الرمز والعنوان، وثلاثة ملفات صُححت محافظًا داخل طبقة النص، وملف «نظام حماية البيئة» المستخرج من الصفحات 89-95 من الملف المجمع.

- رُبط 27 توصيفًا بالموقع بعد تطابق الرمز الكامل والاسم معًا، بإجمالي 40 نطاقًا في الخطتين 38 و47.
- صُحح «القانون الدولي العام» من `2003006-3` إلى `2003226-3`، و«النظام الجزائي العام» من `2003223-3` إلى `2003222-3`، وربط كل منهما بنطاقي الأنظمة 38 و47.
- شملت المطابقة حالتين لا يختلف فيهما الاسم إلا بالهمزة، بعد تطبيع إملائي محافظ موثق في `manifest.json`.
- استُبعدت 7 توصيفات من الربط التلقائي: 5 حالات تعارض في الاسم أو الرمز، وحالتان بلا مطابقة.
- صُحح عنوان `2003404-2` إلى «تدريبات قانونية» وربط بخطة الأنظمة القديمة 38؛ لم يُربط بخطة 47 لأن مقرري التدريب فيها يحملان هويتين مختلفتين.
- أربع مطابقات تحمل تحذير اختلاف الساعات بين التوصيف والخطة؛ لم تُغيّر ساعات الخطة.
- جداول الاعتماد مكتملة في الملفات الـ34.
- لا يظهر وسم «مقيد | Restricted» أو `Confidential`، واتجاه القالب العربي RTL سليم.

يحتوي `manifest.json` على سجل المطابقة والاستبعادات ومصدر كل ملف، ويحتوي `hashes.sha256` على بصمات ملفات PDF. ويحتوي `data.course_details.merge.fragment.json` على المداخل الصارمة الثلاثة الجاهزة للدمج في `data.json.course_details`. استُبعد قسم «تاريخ القانون» من الملف المجمع لأنه تقرير مقرر لا توصيف مقرر.
