# تعليمات العمل المنسق على مشروع توصيفات كلية الشريعة

هذه التعليمات هي نقطة البداية لأي نموذج أو وكيل يعمل على المشروع. اقرأها مع
`WORK_STATUS.md` قبل فحص الملفات أو تعديلها. تعليمات المستخدم المباشرة في المهمة
الحالية أعلى أولوية من هذا الملف.

## المستودع الوحيد المعتمد

- المسار المحلي الوحيد: `/Users/majd/Desktop/codex/courseOfShar3ah-main`
- مستودع GitHub: `https://github.com/majed354/courseOfShar3ah.git`
- الفرع المنشور: `main`
- موقع الإنتاج: `https://courseofshar3ah.netlify.app`
- معرّف مشروع Netlify: `80882d82-86df-4806-b886-320222429d7f`

لا تنشئ نسخة شقيقة أو clone أو worktree جديدًا باسم قريب داخل
`/Users/majd/Desktop/codex`. لا تعمل في مسار «خطط ومقررات كلية الشريعة» القديم،
ولا في `courseOfShar3ah-clo-plo-fix` أو `courseOfShar3ah-complete-extraction`؛ فقد
دُمجت هذه المسارات وأزيلت.

## منع التعارض بين النماذج

1. قبل أي تعديل نفّذ `pwd` و`git remote get-url origin` و`git branch --show-current`
   و`git status --short`، وتأكد من المسار والمستودع والفرع أعلاه.
2. اقرأ `WORK_STATUS.md`، ثم افحص آخر التزامات `main` بواسطة `git log -5 --oneline`.
3. إذا وجدت تغييرات غير ملتزم بها لم تنشئها أنت، فلا تمسحها ولا تستبدلها. افحص
   مالكها وغرضها، واعمل في ملفات غير متعارضة أو انتظر حتى تُسلّم الدفعة.
4. كاتب واحد فقط يغيّر `data.json` أو `course-outcomes.json` أو ملفات PDF في الوقت
   نفسه. لا تبدأ دفعة متداخلة على هذه الملفات.
5. اجعل كل دفعة محددة، اختبرها، التزم بها في Git، ارفعها، ثم حدّث
   `WORK_STATUS.md`. لا تترك مصدر الحقيقة موزعًا بين مجلدات محلية.
6. لا تحذف الملفات غير المتعقبة؛ فهي مصادر المستخدم الخام، وليست مخلفات تلقائية.

## مصادر الحقيقة

- الخطط والمقررات وروابط التوصيف: `data.json`
- طبقة نواتج المقررات: `course-outcomes.json`
- ملفات التوصيف المنشورة: `assets/course-specifications/**/*.pdf`
- سجل مطابقة التوصيفات والتغطية: `COURSE_SPECIFICATIONS_AUDIT.md`
- عقد استخراج النواتج والتحقق منها: `COURSE_OUTCOMES_EXTRACTION.md`
- جرد المقررات المشتركة: `SHARED_COURSE_PLO_BLANKING_AUDIT.md`
- قائمة العمل التخصصية الحالية:
  `قائمة_المقررات_التخصصية_المفقودة_2026-09-10.xlsx`

الملف الخارجي القديم
`برومت_إكمال_استخراج_التوصيفات.md` مرجع تاريخي لبداية استخراج النواتج، وليس
تعليمات التشغيل الحالية: أرقامه ومساراته ونطاقه تعود إلى مرحلة سابقة، وبعض قيوده
تجاوزتها توجيهات المستخدم اللاحقة التي أجازت تصحيح البيانات وإضافة التوصيفات
المناسبة واقتراح تصحيح ملفات PDF.

## قاعدة المقررات المشتركة

- المقرر المشترك يبقى مقررًا واحدًا، ويمكن أن يرتبط بأكثر من برنامج وخطة بواسطة
  `variants[].scopes`.
- ربط CLO بـPLO خاص بالسياق البرنامجي، ويحفظ لكل برنامج في
  `clos[].plo_mappings[].scope`. لا تحفظ رمز برنامج واحد بوصفه ربطًا عالميًا، ولا
  تنقل ربطًا من برنامج إلى آخر.
- إذا ثبت ربط البرنامج من توصيف البرنامج أو دليل رسمي، يسجل في نطاق ذلك البرنامج
  وحده. مثال مقصود: قد يرتبط المخرج نفسه بـ`ع1` في القرآن وعلومه وبـ`ع3` في
  القراءات.
- إذا كانت خلية ربط البرنامج فارغة عمدًا في توصيف مقرر مشترك، فتبقى الرموز `[]`
  والحالة `explicitly_unmapped` حتى يظهر دليل برنامجي؛ لا تحولها إلى `missing` ولا
  تخمّن رمزًا.
- اختلاف الخطة القديمة والجديدة داخل البرنامج نفسه لا يجعل المقرر مشتركًا بين
  برنامجين؛ هوية البرنامج هي الاسم والدرجة العلمية، مع بقاء الخطة والإصدار ضمن
  النطاق.

## قاعدة التغطية الحالية

عند حساب مقررات الأقسام التخصصية في الكلية:

- يدخل رمز القسم `200*`، ويضاف الاستثناء التاريخي المثبت `101221-2`.
- تستبعد مقررات اللغة العربية والإنجليزية والمتطلبات التابعة لجهات خارج الأقسام.
- تستبعد «الرسالة» و«الاختبار الشامل» من بسط التغطية ومقامها.
- خط الأساس الحالي: 616 ظهورًا متاحًا من 756، و140 ظهورًا مفقودًا تمثل 106
  هويات؛ أي تغطية 81.48% بالظهورات.

لا تنسخ هذه الأرقام يدويًا بعد أي دفعة. أعد حسابها من `data.json` والخطط، ثم حدّث
README وسجل التدقيق وقائمة العمل معًا.

## قواعد التعامل مع التوصيفات

- لا تستبدل توصيفًا قائمًا بملف أقدم أو أقل اكتمالًا.
- طابق الرمز والاسم والساعات والبرنامج والخطة، ولا تعتمد تشابه الاسم وحده.
- لا تخترع نص CLO أو رمز PLO أو وزن تقويم. افتح ملف PDF المطابق للبصمة واقرأ
  الصفحة قبل الحكم بأن المصدر ناقص.
- حافظ على اختلاف الهويات عندما يتكرر الرمز باسمين مختلفين.
- وثق كل استيراد أو استبعاد أو تعديل في manifest وسجل التدقيق المناسب، مع بصمات
  SHA-256 للمصادر ونسخ النشر.
- لا تعدّل `statistics` في `course-outcomes.json` يدويًا؛ تعاد حسابها بالأدوات.

## بوابة التسليم

استخدم بيئة Python فيها `pymupdf`. البيئة المتاحة على هذا الجهاز هي:

```bash
COURSE_PYTHON=/Users/majd/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3
"$COURSE_PYTHON" scripts/verify_course_outcomes.py
"$COURSE_PYTHON" -m unittest discover -s tests
```

لا تُرفع دفعة تغيّر البيانات أو PDF أو الاستخراج قبل نجاح المحقق والاختبارات. خط
الأساس الموحّد اجتاز 119 اختبارًا في 2026-09-10.

## GitHub وNetlify

- `main` هو المصدر المنشور، والدفع إليه يشغّل نشر Netlify تلقائيًا.
- إعداد Netlify يستخدم `publish = "."`، وفي المجلد مصادر خام غير متعقبة؛ لذلك لا
  تنفذ `netlify deploy --prod` محليًا من هذا المجلد حتى لا تُرفع المصادر الخام.
- تحقق من النشر الآلي بعد الدفع، وتأكد أن `commit_ref` يساوي التزام `main` وأن
  الحالة `ready` ولا توجد `error_message`.

