# طبقة مخرجات المقررات المثبّتة

يوثّق هذا الملف كيفية بناء `course-outcomes.json` والتحقق منه. الغرض من الطبقة هو أن تُقرأ ملفات PDF مرة عند التحديث، ثم تُثبّت النتيجة القابلة للمراجعة في المستودع. لا يحتاج مستهلك البيانات بعد ذلك إلى إعادة تحليل PDF، ولا يمكن أن يتغيّر المصدر أو يضاف ملف جديد إلى مخزون التوصيفات من غير أن يكشفه التحقق.

## مصدر الحقيقة واكتشاف النسخ

نقطة البداية هي `data.json`، لا أسماء المجلدات ولا اللاحقة الواقعة بعد `--` في اسم PDF. يبني المستخرج قائمة النسخ المنطقية من `course_details` وفق القواعد الآتية:

أظهر الجرد وقت البناء 371 سجلًا في `course_details`، و21 برنامجًا، و553
سجلًا في `courses_catalog`، و22 قاعدة في `equivalencies`. تُستعمل بيانات
البرامج لإكمال الدرجة عند التطابق الوحيد، وتبقى بيانات الفهرس مرافقة للنسخة؛
ولا تُستعمل قواعد التكافؤ لدمج رموز مقررات مختلفة.

- إذا احتوت النسخة `scopes` استُخدمت القائمة كلها، وإذا احتوت `scope` حُوّل الكائن إلى قائمة من عنصر واحد.
- تُطبّع `version` إلى نص، وتكتمل `degree` الناقصة فقط عندما يحسم التطابق الدقيق مع سجل واحد في `programs` الدرجة بلا التباس.
- السجلات القديمة التي لا تحتوي قائمة `variants` تُربط بالبرامج بتطابق **رمز المقرر مع الاسم المطبع تطابقًا دقيقًا**. لا يكفي تشابه الاسم، ويوقف المستخرج العمل إن لم يجد نطاقًا موثوقًا.
- النسخ الموسومة `catalog_id` من غير نطاق تبقى `scopes: []`؛ لا يُخترع لها برنامج.
- تُحتفظ حقول الفهرس `title` و`summary` و`specification_code` و`match_status` و`match_note` و`catalog_id` داخل `catalog` بالقدر الموجود في المصدر.
- لا تُدمج مقررات مختلفة الرموز ولو تطابقت أسماؤها.

أظهر جرد الشجرة أن اللاحقة بعد `--` وسمٌ بشري للحزمة، وليست صيغة عامة يمكن
تحليلها بأمان. ففي حزمة التصحيحات يربط `manifest.json` وسومًا مثل
`sharia-all` و`sharia-new-v47` بقائمة `selectors` فعلية، بينما تسجل حزمة حسم
الهويات لكل ملف نطاقًا كاملًا مستقلًا. لذلك لا يحوّل المستخرج اسم الملف إلى
نطاق؛ بل يأخذ النطاق من `data.json` ويستعمل البيان المربوط بالبصمة للتحقق من
انطباق التصحيح فقط.

سبق التصميمَ جردُ شجرة Git الكاملة ومراجعة `README.md` وملفات التدقيق
`CLO_PLO_CORRECTIONS_DETAILED_AUDIT.md` و
`CLO_PLO_NEW_PROGRAM_UNIFIED_AUDIT.md` و
`SAME_IDENTITY_UNIFICATION_AUDIT.md` و`COURSE_SPECIFICATIONS_AUDIT.md`، ثم
ملفات README/manifest المتاحة داخل حزم التصحيح. غياب README من حزمة بعينها
سُجل بوصفه واقع الشجرة، ولم يُعوّض بافتراض اسم أو نطاق.

وقت بناء هذه الطبقة كان الجرد الفعلي كما يأتي:

- `clo-plo-corrections-20260901` يحتوي **31 PDF** و`manifest.json`، ولا يوجد
  فيه `README.md`؛ أي إن وصفه بأنه 33 ملف PDF لا يطابق شجرة Git الحالية.
  يشير `data.json` إلى 24 من نسخ التصحيح، وتسجل النسخ السبع الأخرى في دفتر
  الاستبعاد لأنها نسخ نطاقية لم يحددها مصدر بيانات الموقع.
- `identity-resolutions-20260829` يحتوي ثمانية PDF موسومة، وتشير البيانات إلى
  الثمانية؛ النطاقات مأخوذة من البيانات ومتسقة مع `manifest.json`.
- `reference-exclusion-cleanup-20260901` لا يحتوي نسخ PDF داخل المجلد؛ يحتوي
  `README.md` و`manifest.json` و`hashes.sha256`، وتشير سجلاته إلى سبعة ملفات
  نهائية بقيت في مجلداتها الأصلية. مسارات `data.json` تشير إلى تلك الملفات
  النهائية لا إلى نسخ قديمة داخل مجلد التنقية.

وبذلك تكون **401** بصمة PDF نشطة فريدة مشتقة من 406 نسخ منطقية، مع 60 PDF
غير مرجعية مسجلة صراحة في `excluded_sources`. لا يعد الملف غير المرجعي
مخرج مقرر نشطًا، لكن إضافته أو حذفه أو تغيير بصمته يكسر التحقق إلى أن يراجع
دفتر المخزون ويعاد بناؤه.

لكل نسخة `variant_id` ثابت يساوي SHA-256 للتمثيل JSON القانوني، بترتيب المفاتيح ومن دون مسافات، للكائن الآتي:

```json
{
  "course_code": "…",
  "source_pdf": "…",
  "scopes": [{"program": "…", "degree": "…", "plan_type": "…", "version": "…"}],
  "catalog": {"title": "…"}
}
```

لذلك يتغير المعرّف عند تغير هوية النسخة أو نطاقاتها أو بيانات فهرسها، ولا يتغير لمجرد إعادة الاستخراج من PDF نفسه.

## لماذا استُخدمت `plo_mappings`

عدّل المخطط المقترح حقل `plo` المفرد إلى `plo_mappings` داخل كل CLO. السبب أن الواقع في المستودع يتخذ شكلين:

1. ملف موحّد يخدم أكثر من برنامج، وتحتوي خلية الربط فيه أسطرًا من نوع `اسم البرنامج: رمز الناتج`. قد يختلف الرمز للمخرج نفسه من برنامج إلى آخر.
2. ملفات مستقلة للبرامج المختلفة، وتظل كل نسخة مرتبطة بملفها ونطاقها ولا تُدمج اعتمادًا على الاسم.

يمثل كل عنصر في `plo_mappings` نطاقًا واحدًا، ويحمل `plo_codes` بوصفه قائمة
قابلة لأن تكون `null`، مع الحالة والدليل وصفحة المصدر والثقة. القائمة لازمة
لأن صفًا ماديًا واحدًا قد يرتبط بأكثر من ناتج برنامج. ودلالات الحالات هي:

- `mapped`: قائمة `plo_codes` غير فارغة؛ قد تحتوي رمزًا واحدًا أو أكثر كما
  طُبع في الخلية نفسها.
- `explicitly_unmapped`: الملف المنشور يضع شرطة صريحة لهذا البرنامج، ولذلك
  تكون `plo_codes: []`؛ القائمة الفارغة هنا قيمة مصدر صريحة وليست فشلًا.
- `not_present_for_scope`: الخلية الموحّدة لا تشمل ذلك النطاق؛ ينطبق هذا خصوصًا على النطاق القديم عندما تكون الخلية المنشورة مخصصة للخطط الجديدة.
- `missing`: لم توجد قيمة غير ملتبسة، وتكون `plo_codes: null`.
- `conflict`: تعارضت قيم معنونة للبرنامج نفسه، وتكون `plo_codes: null`.
- `ambiguous_for_scope`: ظهر رمز مفرد، لكن الملف يخدم أكثر من برنامج ولم
  يسمِّ الخلية برنامجًا؛ لذلك لم تُنسخ الرموز إلى أي برنامج بالتخمين، وتكون
  `plo_codes: null`.

تكون `plo_codes` كذلك `null` في `not_present_for_scope`. لا تُستعمل `[]` لأي
قيمة لم تُستخرج؛ فهي محجوزة للشرطة الصريحة في المصدر.

لا تُنسخ قيمة برنامج إلى برنامج آخر، ولا يرث نطاق قديم ربطًا منشورًا للنطاق الجديد.

## بنية الملف

البنية الفعلية ذات المستوى الأعلى هي:

```json
{
  "schema_version": "course-outcomes-v1",
  "generated_at": "…",
  "extractor": {
    "name": "course-outcomes-extractor",
    "version": "1.0.0",
    "script": "scripts/extract_course_outcomes.py",
    "script_sha256": "…",
    "pdfplumber": "…",
    "pymupdf": "…",
    "tesseract": "…"
  },
  "source_data": {"path": "data.json", "sha256": "…"},
  "auxiliary_sources": [],
  "courses": {
    "رمز المقرر": {
      "variants": [{
        "variant_id": "…",
        "scopes": [],
        "catalog": {},
        "source_pdf": "assets/course-specifications/…pdf",
        "source_sha256": "…",
        "extracted": {},
        "overrides": []
      }]
    }
  },
  "excluded_sources": [],
  "statistics": {}
}
```

تحتوي `extracted` على:

- `course_name` و`course_name_metadata`، وفي البيانات الوصفية الثقة وصفحة المصدر وطريقة الاستخراج.
- `clos`؛ ولكل سجل `code` و`text` و`assessment` و`assessment_source_page` و`plo_mappings` و`document_plo_codes` و`confidence` و`source_page` و`extraction_method`. تشير `source_page` إلى صف الناتج، بينما تسجل `assessment_source_page` صفحة طريقة التقييم لأنها قد تُقرأ من جدول مباشر لاحق؛ تكون عددًا صحيحًا فقط عند وجود `assessment` وإلا تكون `null`. وإذا طبع المصدر علامة ناقصة مثل `...` أو `3...` تحفظ حرفيًا في `source_code` وتبقى `code: null`، أما الرمز الذي تعذر قراءته فلا يُنشأ له `source_code`.
- `assessment_plan`؛ ولكل بند `label` و`weight` و`source_page` و`confidence`.
- `assessment_plan_total` و`assessment_plan_complete`.
- `warnings` المنظمة، و`extraction_status`، و`page_count`.

كل صفحة مسجلة في `source_page` تبدأ من الرقم 1 كما يراها المراجع البشري. وإذا تعذر إثبات قيمة، تُكتب `null` ويضاف تحذير بدل ملء الفراغ بالتخمين.

حالة النسخة `complete` تعني وجود اسم مقرر ونطاق واحد على الأقل وCLO واحد على
الأقل، وأن لكل CLO رمزًا ونصًا وطريقة تقييم مباشرة، وألا يوجد ربط بحالة
`missing` أو `conflict` أو `ambiguous_for_scope`، وأن تكون كل تسميات وأوزان
خطة التقييم موجودة ومجموعها 100. وجود CLO مع نقص أي من ذلك يجعل الحالة
`partial`. وعدم استخراج أي CLO غير صوري يجعلها `failed`. يبقى ترتيب CLO هو
ترتيبها المنشور في الصفحات؛ لا يعيد المستخرج ترتيب الرموز عدديًا، لأن بعض
المصادر نفسها تطبعها بترتيب مختلف. وإذا طبع المصدر رمز CLO نفسه لصفين
جوهريين مختلفين، يحفظهما المستخرج كما هما ولا يعيد ترقيم أحدهما، ويضيف
`duplicate_source_clo_code` مفهرسًا لكل صف ويجعل النسخة `partial`.

## طبقتا الاستخراج والتصحيح البشري

يعيد كل تشغيل بناء `extracted` بالكامل. أما `overrides` فهي طبقة بشرية منفصلة
يحملها المستخرج كما هي من الملف السابق بواسطة `variant_id`. إذا اختفت نسخة
لها تصحيحات، أو تغيرت بصمة PDF لنسخة لها تصحيحات، أو تغير موضع السجل الذي
يرتكز عليه مسار التصحيح، يرفض المستخرج التشغيل كي لا يحذف التصحيح أو يطبقه
على قيمة أخرى صامتًا.

صيغة التصحيح:

```json
{
  "field": "clos[2].plo_mappings[0].plo_codes",
  "value": ["م1"],
  "reason": "مقارنة الخلية بالصورة الأصلية في الصفحة المحددة",
  "author": "اسم المراجع",
  "date": "2026-09-02"
}
```

لإضافة تصحيح:

1. حدّد النسخة بواسطة `variant_id` وافتح PDF المسجل في `source_pdf` وصفحة المصدر.
2. أضف عنصرًا إلى `overrides` فقط؛ لا تعدّل `extracted` يدويًا.
3. اجعل `field` مسارًا موجودًا فعلًا داخل `extracted`. يدعم المسار مفاتيح الكائنات وفهارس القوائم مثل المثال أعلاه.
4. اكتب سببًا قابلًا للتدقيق واسم المراجع وتاريخًا حقيقيًا بصيغة `YYYY-MM-DD`. يمكن أن تكون `value` نصًا أو رقمًا أو `null` بحسب الحقل. تصحيح PLO يستبدل `plo_codes` كله بقائمة رموز فريدة مطبعة، أو `[]` للفراغ الصريح، أو `null` لعدم ثبوت قيمة؛ لا يدعم تصحيح عنصر منفرد داخل القائمة.
5. شغّل أداة التحقق. ترفض الأداة الحقول الناقصة، والمسارات غير الموجودة، والتاريخ غير الصحيح، وتكرار تصحيحين للمسار نفسه في النسخة.

المستهلك النهائي يبدأ بنسخة عميقة من `extracted` ثم يطبّق عناصر `overrides`
بالترتيب، بتعيين `value` في `field`. لا تُدمج الطبقتان داخل الملف المثبّت، لأن
الفصل هو الضمان الذي يمنع محو المراجعة البشرية عند إعادة الاستخراج. تبقى
حالة الاستخراج والثقة والتحذيرات وصفًا للطبقة الآلية الأصلية؛ وعلى المستهلك
أن يبيّن للمستخدم أن الحقل النهائي مصحح بشريًا بدل إعادة وسم الأصل تلقائيًا.

## معالجة PDF والنص العربي

تستعمل الأداة `pdfplumber` لاكتشاف الجداول وصناديق الخلايا، ثم تعيد ترتيب المحارف هندسيًا. في السطر العربي تُقرأ مجموعات المحارف من اليمين إلى اليسار مع إبقاء سلاسل الأرقام والرموز واللاتينية بالترتيب المنطقي. وتُحفظ مجموعات المحارف ذات العرض الصفري كي لا تتكسر لامات الألف والروابط الطباعية. تُستعمل قراءة الجداول العادية فقط بوصفها مسارًا احتياطيًا عندما لا تتوافر الجداول الهندسية.

عندما تختفي الفواصل المنقطة بين الصفوف من بنية PDF، يبني المستخرج صفوفًا احتياطية من مواضع كلمات رمز CLO. تبقى هذه الصفوف `word_baseline` أدنى أولوية من أي صف هندسي أصلي، ولا يُستعمل قرب رمزين متشابهين لدمجهما. لا يعد صندوقان خلية واحدة إلا إذا تطابقا أو احتوى أحدهما الآخر هندسيًا؛ وبهذا تُجمع الأجزاء التي كشفها `pdfplumber` مرتين لخلية مدمجة، بينما يبقى تكرار الرمز المنشور في صفين منفصلين محفوظًا ومعلّمًا.

ولربط PLO في الجداول المجزأة، يعيد المستخرج بناء حدود الصفوف من الخطوط
الأفقية العريضة بعد تجميع إحداثياتها المتقاربة، ثم يقارن نموذج حدود الجدول
كله بنموذج حزمة عمودي CLO/PLO ويقبل النتيجة الوحيدة الأعلى تغطية. تُربط
النتيجة بوقوع صندوق المحرف داخل الصف المادي، لا بمجرد قرب خط الأساس. ولا
تنتشر خلية PLO مدمجة إلى عدة CLO إلا إذا غطت 80% على الأقل من اتحاد صفوفها،
ولم يقطعها فاصل PLO داخلي، ولم تعبر عنوان مجال مثل `2.0`. وإذا اختلف تفسيران
هندسيان للخلية نفسها، أو اختلف تحليل الصفحة عن التحليل المحلي، يُرفض
التفسيران بدل اتحادهما أو ترجيح أحدهما بلا دليل. وتُسجل الاستعادة الناجحة
بـ`plo_mapping_recovered_from_row_geometry`.

إذا وجد جدول ملخص للمخرجات وجدول لاحق لطرق التقييم المباشر، فلا يسمح للجدول الثاني بإنشاء CLO أو تغيير نصه أو موضعه. يظل الملخص مصدر الصف والترتيب، ويمنح جدول التقييم قيمة `assessment` فقط عند اتفاق المانحين وفرادَة الرمز في الجدولين؛ وتسجل صفحة المانح مستقلة في `assessment_source_page`.

تجري كذلك عمليات التطبيع الآتية قبل التفسير:

- تحويل الأرقام العربية والفارسية `٠١٢٣٤٥٦٧٨٩` و`۰۱۲۳۴۵۶۷۸۹` إلى ASCII.
- تحويل U+00A0 إلى مسافة عادية وتوحيد بعض بدائل الحروف العربية وإزالة التطويل والمحارف الضابطة غير المرئية.
- التعامل مع `،` و`؛` وعلامات الترقيم عند التنظيف والفصل، من غير اعتبارها جزءًا من رمز CLO أو PLO.
- حذف كل `(cid:N)` ومعاملته كفراغ، مع تحذير `cid_removed` يحمل الصفحة وCLO عندما أمكن. لا يُفسر `N` على أنه نص أو رقم مستخرج.
- فحص الأشكال العربية العرضية، وتكرار المحارف غير المعقول، وتفتت الحروف، واختلاط النصوص المشوش. يحدد صندوق نص CLO أفقيًا من عمود العنوان، ورأسيًا من صندوق رمز الصف واتحاد شظايا النص؛ فلا يصبح اتحاد المحارف المقروءة نفسه حدًا يقص السطر الأول أو الأخير. عند فساد طبقة المتجهات تُقسم الخلية إلى أسطر مادية، ويجب أن يكون لكل سطر دليل متجهي سليم أو إجماع OCR مستقل عبر مقياسين ونمطي تقسيم على الأقل. لا تقبل قراءة خلية كاملة أسقطت سطرًا، ولا يصحح المستخرج كلمة لغويًا، وإذا بقي تعارض حرف أساسي أو فقد سطر جوهري تُترك القيمة `null`. وتحفظ علامة الوقف النهائية إذا كانت جزءًا من الخلية المنشورة.

وفي عدد محدود من خلايا PLO يكون الرقم المتجهي صحيحًا بينما تسجل خريطة
`ToUnicode` محرف البادئة العربية خطأً أو بوصفه مسافة. لا يعيد المستخرج قراءة
الرقم بـOCR ولا يستنتجه من التسلسل؛ يعزل صندوق محرف البادئة وحده، ويشترط
إجماعًا مستقلًا عبر عدة مقاييس وأنماط تقسيم. وفي حالة المحرف المسجل مسافة
يشترط كذلك أن يكون محرفًا واحدًا عريضًا، ملاصقًا للرقم من جهة RTL وعلى خط
أساسه نفسه. يرفض تعدد المحارف أو تعارض التصويت، وتسجل القيمة المقبولة
بـ`plo_prefix_recovered_by_ocr`.

يُستعمل PyMuPDF في إنشاء القصاصات المتجهة/الصور، ويُستعمل Tesseract بالعربية والإنجليزية عند الحاجة إلى OCR موجهة. تسجل إصدارات الأدوات في رأس الملف لتكون بيئة البناء قابلة للمراجعة.

## المصادر المساعدة المرتبطة بالبصمة

يقرأ المستخرج ملفين مثبتين في المستودع:

- `assets/course-specifications/unified-new-program-mappings-20260901.json` لربط البرامج داخل الخلايا الموحدة المنشورة.
- `assets/course-specifications/clo-plo-corrections-20260901/manifest.json` لخلايا تصحيحات CLO/PLO المنشورة.

هذه الملفات ليست بابًا لتمرير قيم إلى PDF متغير: قبل استعمال أي سجل، يقارن المستخرج `output_sha256` فيه بالبصمة الفعلية للـPDF. الملف المفقود أو البصمة المختلفة خطأ يوقف التشغيل. كما تُثبت بصمة الملف المساعد نفسه في `auxiliary_sources`.

تُعامل روابط PLO المستعادة من بيان مساعد مطابق للبصمة على أنها `medium`:
البيان يثبت المقصود المنشور ويحل تخطيطات الخلايا المركبة، لكنه ليس قراءة
هندسية مستقلة ثانية. أما نص `text_to` الصريح في بيان التصحيح المطابق للبصمة
فيُعامل على أنه `high` لأنه القيمة المنشورة النهائية نفسها؛ ولا يُستعمل
`text_from`، فهو محدد للبحث لا قيمة خرج. ولا يُستعمل اسم الملف أو اسم المقرر
لاستنتاج نص أو رمز PLO.

## معيار `confidence`

الثقة مبنية على طريقة قابلة لإعادة الفحص، وليست تقديرًا انطباعيًا. وهي تخص القيمة المجاورة لها، لا النسخة كلها:

| القيمة | `high` | `medium` | `low` |
|---|---|---|---|
| اسم المقرر | قراءة متجهية/هندسية اجتازت التطابق مع عنوان الفهرس | OCR للصفحة اجتازت عتبة التطابق مع الفهرس | لم تثبت قيمة، وتكون `course_name: null` |
| نص CLO | قيمة `text_to` صريحة من بيان تصحيح مطابق لبصمة PDF | قراءة هندسية سليمة، أو OCR موجهة سليمة | جدول عادي/خلية عادية احتياطية، أو فشل اختبارات المتجه وOCR؛ في الحالة الأخيرة يكون النص `null` مع تحذير |
| ربط PLO | رمز أو قائمة رموز صحيحة في الخلية الهندسية نفسها، أو قيمة معنونة باسم البرنامج داخل الخلية | استعادة محافظة من حدود الصف المادي، أو OCR معزولة لبادئة ذات رقم متجهي ثابت، أو بيان تصحيح/ربط موحد مطابق للبصمة، أو دليل جدول مباشر مطابق داخل الوثيقة نفسها، أو إثبات أن النطاق غير حاضر في خلية موحدة للخطط الجديدة | قيمة مفقودة أو متعارضة؛ تكون `plo_codes: null` ولا يُخترع الرمز |
| بند خطة التقييم | اسم النشاط ووزنه من الأعمدة الهندسية المحددة | خلية عادية احتياطية، أو استعادة الاسم أو الوزن بـOCR موجهة بعد فشل/فساد طبقة النص | تعارض القراءة المتجهية وOCR أو تعذر قراءة الاسم/الوزن؛ تكون القيمة المتعذرة `null` |

رمز CLO لا يُقبل إلا إذا طابق صيغة صف واضحة مثل `1.1` أو `2.3`، أو الصيغة المنشورة القديمة ذات المجال والرقم مثل `ع1` و`م2` و`ك3` و`ق1`. لا يحوّل المستخرج إحدى الصيغتين إلى الأخرى ولا يملأ رقمًا مفقودًا من التسلسل. صفوف العناوين `1.0` ونظائرها لا تُثبت كمخرجات، والصف الصوري الخالي من النص والربط والتقييم يُحذف مع تحذير. وجود عدة رموز PLO في صف مادي واحد ذي نطاق برنامج واحد يثبت كقائمة؛ أما الملف متعدد البرامج فلا تُسند قائمته غير المعنونة إلى أي برنامج.

## حد معروف في ملفات `quranic-studies-master-1445-approved`

طبقة ToUnicode للأرقام في ملفات هذا المجلد غير موثوقة. لذلك لا يثبت المستخرج رمز PLO عدديًا من النص المتجهي في هذه الملفات، حتى لو بدا كسلسلة صالحة شكليًا. يسجل `plo_numeric_text_untrusted` ويترك `plo_codes: null` ما لم يوجد دليل آخر موثوق. وفي خطة التقييم يحاول OCR موجهة لكل صندوق وزن؛ إذا لم تستعد كل الأوزان بلا التباس، تصبح الأوزان `null` ويُسجل التحذير المناسب. هذا قيد مقصود يمنع تثبيت رقم خاطئ، وليس إذنًا لاستنتاجه من السياق.

## البصمات ومخزون الملفات الكامل

لكل نسخة نشطة `source_sha256`. وتحتوي `source_data` على بصمة `data.json`، بينما تحتوي `auxiliary_sources` على بصمات البيانات المساعدة. ويسجل الرأس مسار المستخرج وبصمته في `extractor.script` و`extractor.script_sha256`؛ يرفض التحقق ناتجًا بُني بنص مختلف من السكربت ولو بقي رقم الإصدار الاسمي ثابتًا.

كل PDF تحت `assets/course-specifications/` لا يشير إليه `data.json` يُسجل في `excluded_sources` مع بصمته وسبب استبعاده. هذا الدفتر لا يعني أن الملف حُذف أو أن محتواه غير صالح؛ معناه فقط أنه ليس مصدر نسخة نشطة في بيانات الموقع الحالية. ويمنع ذلك ملفًا جديدًا أو قديمًا غير محسوب من المرور بصمت.

يتحقق `scripts/verify_course_outcomes.py`، باستخدام مكتبة بايثون القياسية فقط، من الآتي:

- إعادة بناء النسخ والنطاقات ومعرفات `variant_id` من `data.json` بالقواعد نفسها.
- التطابق الكامل بين النسخ المتوقعة والمثبتة، بما في ذلك `scopes` و`catalog` والمسار.
- وجود كل مصدر وبصمته الفعلية، وبصمات `data.json` والمصادر المساعدة.
- تطابق اسم المستخرج وإصداره ومساره وبصمة نص السكربت مع الملف الفعلي.
- أن اتحاد المصادر النشطة و`excluded_sources` يساوي مخزون PDF في المجلد تمامًا، بلا تقاطع أو فجوة.
- سلامة الشكل الأساسي للحقول المستخرجة، وصفحات المصدر، وقيم الثقة، وعدم بقاء U+00A0 أو أرقام عربية أو `(cid:N)` في القيم المثبتة.
- سلامة طبقة `overrides` ومساراتها وتواريخها.
- إعادة حساب الإحصاءات، بما فيها الروابط ذات القوائم غير الفارغة، ومجموع
  تعيينات الرموز، والروابط متعددة الرموز، وتوزيع حالات PLO والنسب ذات المقامات
  المعلنة، وعدم قبول رقم ملخص لا يطابق السجلات الفعلية.

أي اختلاف أو ملف مفقود أو غير ممثل يعيد رمز خروج غير صفري. ويعمل الفحص نفسه في `.github/workflows/verify-outcomes.yml` عند كل `push` وكل `pull_request`.

## التشغيل وإعادة البناء

من جذر المستودع، وببيئة Python تحتوي `pdfplumber` وPyMuPDF، مع توفر
Tesseract وحزمتي اللغة `ara+eng`:

```bash
python3 scripts/extract_course_outcomes.py \
  --repo-root . \
  --data data.json \
  --specifications-dir assets/course-specifications \
  --output course-outcomes.json \
  --jobs 8

python3 scripts/verify_course_outcomes.py
```

يمكن تمرير `--generated-at` لتاريخ ISO-8601 ثابت. عند عدم تمريره، تستخدم الأداة `SOURCE_DATE_EPOCH` إن كان مضبوطًا، وإلا تستخدم تاريخ آخر التزام Git مسّ `data.json` أو مجلد التوصيفات. يُكتب JSON بترتيب مفاتيح ثابت إلى ملف مؤقت ثم يستبدل ذريًا؛ لذلك تعطي المصادر والأدوات والمعاملات نفسها بايتات متطابقة.

بعد أي تغيير في `data.json` أو PDF أو أحد الملفين المساعدين، أعد الاستخراج ثم نفّذ التحقق. لا تعدّل بصمة يدويًا كي تجعل الفحص يمر.

## نتائج التشغيل النهائي

هذه أرقام الملف المثبت بعد تشغيلين كاملين متتاليين بالأمر نفسه والبيئة نفسها:

| المقياس | النتيجة النهائية |
|---|---:|
| رموز المقررات | 371 |
| النطاقات المطبعة | 651 |
| النسخ المنطقية | 406 |
| مصادر PDF النشطة / المستبعدة المسجلة | 401 / 60 |
| النسخ المكتملة / الجزئية / الفاشلة | 44 / 362 / 0 |
| صفوف CLO المثبتة | 2,859 |
| رموز CLO المقروءة | 2,842 / 2,859 (99.41%) |
| نصوص CLO المقروءة | 2,283 / 2,859 (79.85%) |
| طرق التقييم المباشر المقروءة | 1,921 / 2,859 (67.19%) |
| CLO له PLO واحد على الأقل | 2,186 / 2,859 (76.46%) |
| روابط PLO غير الفارغة / الروابط الواجبة التطبيق | 2,995 / 4,004 (74.80%) |
| مسميات خطة التقييم المقروءة | 1,204 / 1,368 (88.01%) |
| أوزان خطة التقييم المقروءة | 1,326 / 1,368 (96.93%) |
| روابط PLO متعددة الرموز | 22 |

مقام روابط PLO الواجبة التطبيق لا يشمل 147 شرطة منشورة صريحة
(`explicitly_unmapped`) ولا 408 حالات لا يظهر فيها البرنامج أصلًا في الخلية
الموحدة (`not_present_for_scope`). أما المقام 2,859 في مقياس CLO ذي PLO فهو
عدد صفوف CLO نفسها، بصرف النظر عن عدد نطاقات كل نسخة.

كان ناتج التشغيلين متطابقًا بايتًا ببايت (`cmp` أعاد صفرًا)، وكانت بصمة
SHA-256 في كليهما:

```text
ca9cc8f7eaa3f0306d30b0d5b90589f13b37790e1b497b0ba5b0661640a8d4b1
```

## التدقيق البصري لعينة من 20 ملفًا

نُسخت الحقيقة المرجعية يدويًا من صفحات PDF المرسومة، قبل مقارنة خرج
المستخرج؛ ولم يُعامل خرج المستخرج نفسه على أنه حقيقة. تغطي العينة 20 مصدرًا
في 13 عائلة مجلدات، وتشمل ملفات جذرية، خامًا مستعادًا، تصحيحات PLO، حسم
هويات، برامج قديمة وجديدة، وملف ToUnicode غير موثوق. قورنت 544 ذرة مستقلة:

| بند العينة | الصحيح / المفحوص | النسبة |
|---|---:|---:|
| اسم المقرر | 19 / 20 | 95.00% |
| عدد CLO في الملف | 20 / 20 | 100.00% |
| خانات رمز CLO المرتبة | 149 / 149 | 100.00% |
| نص أول وآخر CLO في كل ملف | 25 / 40 | 62.50% |
| مجموعة رموز PLO الدقيقة لكل CLO | 142 / 149 | 95.30% |
| عدد بنود خطة التقييم | 20 / 20 | 100.00% |
| أوزان خطة التقييم | 67 / 73 | 91.78% |
| مسميات خطة التقييم | 61 / 73 | 83.56% |
| **المعدل الذري الكلي** | **503 / 544** | **92.46%** |

إضافيًا، تطابق ترتيب رموز CLO في 20/20 ملفًا، وتطابقت صفحات نصوص CLO
المفحوصة في 40/40 حالة. بلغت حساسية وجود PLO ‏136/143 (95.10%)، ولم يثبت
المستخرج أي رمز في الخانات الست التي تحقق المراجع من فراغها (6/6). تطابقت
ثمانية ملفات في جميع الذرات؛ أما الملفات الاثنا عشر الأخرى فكان فيها فرق
واحد على الأقل، ويظهر تفصيل الفروق حسب نوع الحقل في الجدول بدل إخفائه داخل
النسبة الكلية. الملف الوحيد الذي بقيت كل رموز PLO السبعة وأوزانه الست فيه
`null` هو عينة ToUnicode المقصودة `2002852-2.pdf`.

ملفات العينة هي:

1. `assets/course-specifications/2003215-2.pdf`
2. `assets/course-specifications/quranic-course-updates-1446/2002252-2-ulum-alquran-general.pdf`
3. `assets/course-specifications/raw-recovery-20260827/103171-2.pdf`
4. `assets/course-specifications/raw-recovery-20260827/2001112-2.pdf`
5. `assets/course-specifications/clo-plo-corrections-20260901/2002412-2--qiraat-old-v38.pdf`
6. `assets/course-specifications/clo-plo-corrections-20260901/2001110-2--sharia-all.pdf`
7. `assets/course-specifications/clo-plo-corrections-20260901/2001346-2--sharia-new-v47.pdf`
8. `assets/course-specifications/clo-plo-corrections-20260901/2001706-2--usul-master.pdf`
9. `assets/course-specifications/identity-resolutions-20260829/2001106-2--quran.pdf`
10. `assets/course-specifications/identity-resolutions-20260829/2001106-2--qiraat.pdf`
11. `assets/course-specifications/identity-resolutions-20260829/2002454-2--quran.pdf`
12. `assets/course-specifications/identity-resolutions-20260829/2002454-2--qiraat.pdf`
13. `assets/course-specifications/quran-old-v39/2002115-2.pdf`
14. `assets/course-specifications/islamic-studies-bundle-1444/20043101-2.pdf`
15. `assets/course-specifications/fiqh-master-1446/2001832-4.pdf`
16. `assets/course-specifications/qiraat-master-1445/2002812-2.pdf`
17. `assets/course-specifications/local-gap-completion-20260822/quran-culture/2002707-3.pdf`
18. `assets/course-specifications/systems-updates-1446/2003210-3.pdf`
19. `assets/course-specifications/sharia-ba-1445/2001108-2.pdf`
20. `assets/course-specifications/quranic-studies-master-1445-approved/2002852-2.pdf`

## ما تعذر استخراجه ولماذا

لم تفشل أي نسخة بالكامل، لكن 362 نسخة صُنفت `partial` لأن معيار الاكتمال
متعمد الصرامة. ترك المستخرج القيم الآتية `null` ولم يخمنها:

| الحقل | عدد القيم غير المثبتة | السبب المسجل |
|---|---:|---|
| اسم المقرر من PDF | 91 نسخة | لم تتفق القراءة المتجهية أو OCR مع عنوان الفهرس بثقة كافية |
| رمز CLO | 17 / 2,859 | 12 خانة تنشر علامة حذف وتحفظ في `source_code`، و5 خانات غير مقروءة |
| نص CLO | 576 / 2,859 | فساد/تفتت طبقة النص وعدم بلوغ OCR إجماعًا محافظًا |
| طريقة التقييم المباشر | 938 / 2,859 | الخلية فارغة أو لا يوجد مانح جدول مباشر فريد ومتفق |
| مسمى بند خطة التقييم | 164 / 1,368 | خلية غير مقروءة أو OCR غير حاسم |
| وزن بند خطة التقييم | 42 / 1,368 | فساد أرقام ToUnicode في الملفات السبعة المبينة أدناه |
| خطة التقييم كلها | 5 / 406 نسخ | لم يعثر على جدول خطة صالح في المصدر |
| نطاق النسخة | 17 / 406 نسخ | لم يزوّد `data.json` النسخة ببرنامج يمكن إثباته بلا تخمين |

في ربط PLO بقيت 678 خريطة `missing` و331 خريطة
`ambiguous_for_scope`، ولم توجد أي حالة `conflict`. تعني الحالة الملتبسة أن
الملف يخدم أكثر من نطاق وفيه رمز غير معنّون باسم البرنامج؛ ولذلك لم ينسخ
المستخرج الرمز إلى البرامج. التوزيع الكامل للنواقص والالتباسات هو:

| عائلة المسار | مفقود | ملتبس للنطاق | المجموع |
|---|---:|---:|---:|
| جذر `course-specifications` | 238 | 35 | 273 |
| `raw-recovery-20260827` | 33 | 123 | 156 |
| `sharia-ba-1445` | 135 | 0 | 135 |
| `quran-old-v39` | 0 | 84 | 84 |
| `local-gap-completion-20260822` | 35 | 30 | 65 |
| `islamic-studies-bundle-1444` | 0 | 59 | 59 |
| `quranic-studies-master-1445-approved` | 51 | 0 | 51 |
| `graduate-all-20260823` | 49 | 0 | 49 |
| `systems-source-2022` | 40 | 0 | 40 |
| `usul-master-1446` | 38 | 0 | 38 |
| `clo-plo-corrections-20260901` | 18 | 0 | 18 |
| `fiqh-phd-1446` | 17 | 0 | 17 |
| `qiraat-source-1445` | 10 | 0 | 10 |
| `usul-phd-1446` | 8 | 0 | 8 |
| `specialty-recovery-20260826` | 2 | 0 | 2 |
| `fiqh-master-1446` | 2 | 0 | 2 |
| `systems-updates-1446` | 2 | 0 | 2 |
| **المجموع** | **678** | **331** | **1,009** |

حزمة التصحيحات ليست ضمن العجز التقني المعروف للأداة القديمة: ثُبتت 180
خريطة من 198 خريطة واجبة التطبيق فيها. الحالات الـ18 الباقية كلها في
`2001110-2--sharia-all.pdf`، وهي ستة صفوف ذات خلية مصدر فارغة مضروبة في
ثلاثة نطاقات. واستُعيدت بادئتا PLO الفاسدتان في `2001465-2--sharia-new-v47.pdf`
من محرفهما المعزول مع بقاء الرقم المتجهي ثابتًا.

ملفات ToUnicode السبعة التي بقيت فيها 51 خريطة PLO و42 وزنًا بلا قيمة هي:

- `quranic-studies-master-1445-approved/2002852-2.pdf`
- `quranic-studies-master-1445-approved/2002853-2.pdf`
- `quranic-studies-master-1445-approved/2002854-2.pdf`
- `quranic-studies-master-1445-approved/2002855-2.pdf`
- `quranic-studies-master-1445-approved/2002861-4.pdf`
- `quranic-studies-master-1445-approved/2002862-2.pdf`
- `quranic-studies-master-1445-approved/2002864-2.pdf`

النسخ الخمس التي لم يُستخرج لها جدول خطة تقييم هي:

- `raw-recovery-20260827/103213-3.pdf`
- `raw-recovery-20260827/2001237-4.pdf`
- `usul-master-1446/2001707-2.pdf`
- `graduate-all-20260823/law/2001880-2.pdf`
- `law-master-1443/2003814-2.pdf`

أما النسخ غير المسندة إلى نطاق فهي 15 ملفًا تحت
`law-master-1443-catalog-only`، إضافة إلى
`qiraat-master-1445/2002831-4.pdf` و
`quranic-studies-master-1447/2002863-2--manhaj-al-bahth.pdf`.

القائمة الصفّية الكاملة ليست ملخصًا خارجيًا: لكل قيمة غير مثبتة تحذير منظم
داخل نسخة `source_pdf` نفسها، ويحمل `source_page` و`clo_index` أو `clo_code`
حيث ينطبق. يمكن إخراجها للمراجعة مثلًا بالأمر الآتي:

```bash
jq -r '
  .courses | to_entries[] as $course
  | $course.value.variants[] as $variant
  | $variant.extracted.warnings[]?
  | select(
      .code == "course_name_unresolved"
      or .code == "clo_code_missing_in_source"
      or .code == "clo_code_unreadable"
      or .code == "clo_text_unreadable"
      or .code == "direct_assessment_unresolved"
      or .code == "plo_mapping_unresolved"
      or .code == "assessment_plan_missing"
      or .code == "assessment_label_unresolved"
      or .code == "assessment_label_unreadable"
      or .code == "assessment_weight_ocr_failed"
      or .code == "variant_scope_unresolved"
    )
  | [
      $course.key, $variant.source_pdf, $variant.variant_id,
      (.source_page // ""), (.clo_index // ""),
      (.clo_code // ""), .code, .message
    ]
  | @tsv
' course-outcomes.json
```
