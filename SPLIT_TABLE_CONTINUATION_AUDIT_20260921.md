# تدقيق صفوف CLO الممتدة بين صفحتين — 21 سبتمبر 2026

## النتيجة

استعاد المستخرج ستة صفوف `3.1` كانت تبدأ في الصفحة الثالثة وتكتمل خلاياها في
الصفحة الرابعة. كانت المشكلة في قيد تقني يعتمد بصمة ملف PDF للسماح بضم جدولين
متجاورين؛ لم تكن الصفوف مفقودة من المصدر. أصبح كل توصيف من الستة يحتوي ستة
مخرجات مكتملة، ولم يعد تقرير نواتج البرنامج يرى تعارضًا بين ملف القياس الجاهز
وتوصيف المقرر `2002325-2`.

| المقرر | نص 3.1 المستعاد | ربط PLO الفعّال | حالة PDF |
|---|---|---|---|
| `2002124-2` | أن يلتزم الطالب بآداب تلاوة القرآن الكريم أثناء الحفظ والتسميع. | كما في المصدر | مطابق |
| `2002224-2` | أن يلتزم الطالب بآداب تلاوة القرآن الكريم أثناء الحفظ والتسميع. | كما في المصدر | مطابق |
| `2002232-2` | أن يلتزم الطالب بالموضوعية عند دراسة الاختلافات في علوم القرآن. | غير مربوط في طبقة المقرر المشترك | يحتاج تحديث خلية الربط |
| `2002325-2` | أن يلتزم الطالب بآداب تلاوة القرآن الكريم أثناء الحفظ والتسميع. | كما في المصدر | مطابق |
| `2002326-2` | أن يبادر الطالب إلى تطوير مهاراته في تلاوة وفهم القرآن باستمرار. | `ق2` | مطابق |
| `2002456-2` | أن يبادر الطالب إلى تطوير معرفته بأوجه إعجاز القرآن باستمرار. | غير مربوط في طبقة المقرر المشترك، ويطبّق ربط البرنامج في نطاقه | يحتاج تحديث خلية الربط |

## الحوكمة

- قُيّد ضم الصفحات ببصمة SHA-256 لكل ملف في
  `review-batches/split-table-continuations-20260921.json`، فلا ينتقل الحكم إلى
  ملف جديد أو نسخة أخرى تلقائيًا.
- بقيت التحسينات المعتمدة في `overrides` كما هي. إذا اختلفت القيمة المعتمدة في
  JSON عن PDF، يظل الوسم `source_pdf_gap_confirmed` ويفتح بند تحديث PDF؛ لا
  تعاد القيمة القديمة ولا يخفى الاختلاف.
- لم يتغير أي ملف PDF في هذه الدفعة. سجّل النظام توصيتين مفتوحتين للمقررين
  `2002232-2` و`2002456-2` لتحديث خلية ربط PLO لاحقًا.
- عند مزامنة المستودع الرسمي مع مرآة الأتمتة، يحفظ الدمج المرفوعات المحلية
  ومراجعات مواءمة البرنامج، ويؤرشف النسخة الرسمية السابقة عند تغير بصمتها في
  المسار نفسه. كما يسقط إصلاح `clos.append` القديم إذا أصبح الصف موجودًا في
  الاستخراج الرسمي، منعًا للتكرار.

## التحقق

- نجح `scripts/verify_course_outcomes.py`: 410 مقررات، 448 نسخة، 734 نطاقًا،
  و436 مصدر PDF نشطًا.
- نجحت اختبارات المستودع الكاملة: **150 اختبارًا**.
- نجحت مجموعة الانحدار المستهدفة: **39 اختبارًا**.
- نجحت اختبارات مرآة الأتمتة والمواءمة والمصدر: **127 اختبارًا**.
- أعاد فحص نسخة معزولة من مهمة PLO السابقة القرار `ready`، بلا موانع وبعدد
  `READY_CLO_REFERENCE_MISMATCH = 0`، مع بقاء مدخلات القياس الأصلية دون تغيير.
- نُشرت البيانات بالالتزام `5144a0a9bcba3c7876c77797c11f5cd418688f46`،
  وأصبح نشر Netlify `6ab0ff832dde6d000889a09b` بحالة `ready` مع
  `commit_ref` مطابق و`error_message` فارغ.
