# حالة العمل الحالية

آخر تحديث: 2026-09-10.

## حالة الكتابة

`IDLE` — لا توجد دفعة كتابة نشطة. اكتملت دفعة الاسترداد العميق من ملف Google
Drive الجامع، والتزم بها `26ddc26` ودُفعت إلى `main` ونُشرت تلقائيًا.

على النموذج الذي يبدأ دفعة جديدة أن يغيّر الحالة إلى `IN_PROGRESS` ويكتب اسم
المهمة والملفات التي سيعدلها، ثم يعيدها إلى `IDLE` بعد الاختبار والالتزام والرفع.
إذا كانت الحالة `IN_PROGRESS` فلا يبدأ نموذج آخر تعديلًا متداخلًا على الملفات
نفسها.

## آخر نتيجة موحّدة

- دُمجت أعمال استخراج التوصيفات، وإصلاح CLO/PLO، وتفريغ خلايا ربط المقررات
  المشتركة، والاسترداد من الويب، والمراجعة الخارجية للمقررات.
- قاعدة النواتج الحالية: 390 رمز مقرر، و425 نسخة، و700 نطاق برنامجي، و420
  مصدر PDF نشطًا، و133 مصدرًا مستبعدًا مسجلًا.
- نجح `scripts/verify_course_outcomes.py`.
- نجحت حزمة الاختبارات: 123 اختبارًا.
- استُعيدت خمسة توصيفات من ملف جامع ذي 170 صفحة بعد فحصه صفحةً صفحة؛ رُبطت
  بتسعة مواضع في خطط الشريعة والدراسات الإسلامية، واستُخرجت نواتجها بمراجعة
  بصرية مثبتة بالبصمات بدل الاعتماد على OCR المشوه.
- حُدثت قائمة المقررات التخصصية المفقودة بصيغتي Excel وMarkdown.
- آخر التزامات التسليم:
  - `26ddc26`: استرداد خمسة توصيفات من ملف الجامعة الجامع وتحديث التغطية.
  - `4c791a5`: توثيق المستودع الموحّد وقواعد تنسيق العمل.

## التغطية التخصصية

- المطلوب: 756 ظهورًا.
- المتاح: 625 ظهورًا.
- المفقود: 131 ظهورًا، تمثل 101 هوية فريدة.
- التغطية: 82.67%.

قائمة العمل التفصيلية في:

- `قائمة_المقررات_التخصصية_المفقودة_2026-09-10.xlsx`
- `قائمة_المقررات_التخصصية_المفقودة_2026-09-10.md`

## ملفات محلية يجب الحفاظ عليها

توجد داخل المستودع مصادر خام غير متعقبة، منها ملف الخطط القديمة ومجلدات المراجع
المستبعدة وتنسيق المخرجات وتوصيفات البرامج. بقاؤها غير متعقبة مقصود حاليًا؛ لا
تحذفها، ولا ترفعها إلى الموقع، ولا تضفها إلى Git بلا مراجعة نطاقها وأحجامها.

## النشر

- GitHub: `https://github.com/majed354/courseOfShar3ah`، الفرع `main`.
- Netlify: `https://courseofshar3ah.netlify.app`.
- نشر Netlify الآلي للدفعة `26ddc26` بحالة `ready` ومن دون `error_message`،
  وكان `commit_ref` مطابقًا للالتزام الكامل
  `26ddc26d7064b7126963f188699657c941976aa3`.
